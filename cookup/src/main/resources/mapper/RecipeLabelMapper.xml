<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lzkydh.mapper.RecipeLabelMapper">

    <insert id="addRecipeLabelByBatch">
        insert into recipe_label(label_id,recipe_id)
        values
        <foreach collection="labelIds" item="item" index="index" separator="," >
            (#{item},#{recipeId})
        </foreach>
    </insert>

    <delete id="deleteRecipeLabelByRecipeId">
        delete from recipe_label
        where recipe_id = #{recipeId}
    </delete>
</mapper>